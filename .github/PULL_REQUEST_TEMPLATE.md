## What

<!-- One or two sentences: what does this PR change and why? Link the issue if there is one. -->

## Checklist

- [ ] Tests added or updated for the behavior change
- [ ] `uv run ruff format --check && uv run ruff check && uv run mypy src && uv run pytest` passes locally
- [ ] `CHANGELOG.md` updated under **Unreleased** (user-visible changes only)
